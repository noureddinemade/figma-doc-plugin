// Import
import { isArray } from "../functions/general";

// Get the anatomy of the component
export function FUNCTION(component: any) {

    // Set up
    let ARRAY: any[] = [];

    //
    return isArray(ARRAY) ? ARRAY : null;

}