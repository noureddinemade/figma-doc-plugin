// Import
import { isArray } from "../../functions/general";

// Get styles from component, which includes any variants, children and variant children
export function getStyles(component: any) {

    // Set up
    let response: any[] = [];

    //
    return isArray(response) ? response : null;

}